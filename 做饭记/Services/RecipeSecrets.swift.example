import Foundation

/// `baseURL` / `appToken` 只是连到 **做饭记** 自建 BFF；真正的通义 Key 只在服务端 `bff/.env` 的 `DASHSCOPE_API_KEY`。
enum RecipeSecrets {
    static let baseURL = "https://api.smallbeebee.com/v1/recipe/chat/completions"
    static let appToken = "<与 bff/.env 中 APP_TOKEN 一致，与 cycle 无关>"
}
