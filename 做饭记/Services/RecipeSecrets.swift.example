import Foundation

/// 本地密钥（不入库）
///
/// 1. 复制本文件为同目录下的 `RecipeSecrets.swift`
/// 2. 将 `dashScopeAPIKey` 改为你的 DashScope API Key
/// 3. `RecipeSecrets.swift` 已在 `.gitignore` 中，不会被 push
enum RecipeSecrets {
    static let dashScopeAPIKey = ""
}
